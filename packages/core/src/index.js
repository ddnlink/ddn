import getPaths from './getPaths'
import getUserConfig, { requireFile, mergeConfigs } from './getUserConfig'

export {
  getUserConfig,
  requireFile,
  mergeConfigs
}

export default {
  getPaths,
  getUserConfig,
  requireFile,
  mergeConfigs
}
