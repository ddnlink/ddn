import getPaths from './getPaths'
import getUserConfig, { requireFile, mergeConfigs } from './getUserConfig'

export {
  getUserConfig,
  requireFile,
  mergeConfigs
}

// todo: delete it
export default {
  getPaths,
  getUserConfig,
  requireFile,
  mergeConfigs
}
